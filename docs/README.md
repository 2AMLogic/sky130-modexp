# docs

- `baseline.md` — the measured synthesis baseline and its reproduction recipe.
- `environment.md` — pinned tool/PDK versions and cold-start install steps
  (`scripts/setup-env.sh`).
- `signoff-claim.md` — the authoritative DRC/LVS claim against the routed
  GDS: what the evidence in `layout/drc/`/`layout/lvs/` does and does not
  establish, and the resulting reading of `spec/modexp.md`'s Signoff row.
