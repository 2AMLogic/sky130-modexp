<!-- record-meta
{
  "record_id": "20260816-072918-d8eafca",
  "experiment": "place-and-route",
  "supersedes": "20260814-203901-c741877",
  "git_revision": "d8eafcadb65b6d7884af3abd8e4c32097fbfdd1e",
  "provenance": {
    "klt_version": "0.2.0",
    "pdk": {
      "name": "sky130A",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "content_hash": "sha256:8e78e14442062dba34d414fca6490b2f6b96038d4510d1438ca44fee31487135"
    },
    "inputs": [
      {
        "path": "rtl/modexp.v",
        "content_hash": "sha256:109db84055efe603af2e71506aa7630483696d84653b1de4c0c1d05171b8ed93"
      },
      {
        "path": "flow/tie_constants.py",
        "content_hash": "sha256:ed02a26d6ffc357bd36bc76461004797bf34d1b69bbb0fca5a3a873f92eb5b17"
      },
      {
        "path": "flow/synthesize-modexp.json",
        "content_hash": "sha256:6b86cf02f7aa159a07099f1b74b4cda1f494a568cda373958731bc279df44e30"
      },
      {
        "path": "flow/run-corner-sweep.sh",
        "content_hash": "sha256:108db15e54846a13543e9e54534fec519cf9660787e9eab524f31ca79d6c0e1f"
      }
    ]
  }
}
-->

# Record 20260816-072918-d8eafca

- **Record ID**: 20260816-072918-d8eafca
- **Claim**: issue #56's mapping-only remediation step toward
  `spec/decision-records/0002-slow-corner-timing-closure-and-mm-red-critical-path.md`
  Decision 3's named first action: a fresh eighteen-corner
  `klt place-and-route` sweep (same ratified corner matrix,
  `spec/decision-records/0001-input-domain-interface-and-corner-matrix.md`
  Decision 4) of the **same, unmodified** `rtl/modexp.v` netlist, placed and
  routed with a **revised floorplan** (`flow/run-corner-sweep.sh`:
  utilization 55%, `core_margin_um` 2 — vs. the prior record's 35%/4), no
  cell-library, IO-layer, clock, or seed change. **No RTL change**: `git
  diff` against `rtl/modexp.v` between this record's commit and the
  superseded record's commit is empty (verified — see "Numbers in this
  record" below). This record measures how much of the previously-recorded
  slow-corner timing gap a mapping-only change closes, per issue #56's own
  framing: **not expected to close the ~4.2× gap alone**, only to
  establish how much of it is mapping-attributable vs. architecture-bound.
- **Design provenance**: netlist (post-synthesis, byte-identical to the
  superseded record — `flow/.klt/synthesize/modexp_synth_tied.v`,
  content_hash `sha256:67a218e16cf51e4f3010e5f87404b829f0574d9765b2569278abd54a4fdc7486`,
  matching the superseded record's netlist hash exactly), derived from
  `rtl/modexp.v` @ `c741877543658a0974a8a90f07cd4b37a7da4aa1` (unchanged
  since the superseded record; this record's own `git_revision` above is
  later only because of unrelated intervening commits, not an RTL change —
  confirmed by the identical netlist hash and by
  `python3 verification/cross_check.py` passing 2000/2000 at every verified
  `WIDTH` on this record's own commit, per `CLAUDE.md`'s correctness gate).
  **`layout/modexp.def`/`layout/modexp.gds` are *not* regenerated by this
  record** — they still reflect the *original* (pre-#56) floorplan, and
  their own DRC/LVS evidence (`verification/records/drc-lvs/`) is untouched
  and still valid against them; regenerating those signed-off artifacts
  against this record's revised floorplan is separate, out-of-scope future
  work. This record's own routed DEF/GDS per corner are scratch-only
  (`flow/corners/<corner>/.klt/place-and-route/`, gitignored); only the
  metrics and one supplementary critical-path report are frozen as
  artifacts here.
- **Run configuration**:
  1. `klt synthesize` (engine `yosys`) against `flow/synthesize-modexp.json`
     — unchanged from the superseded record (unconstrained synthesis,
     byte-identical output, verified by the matching netlist hash above).
  2. `python3 flow/tie_constants.py` — unchanged.
  3. `flow/run-corner-sweep.sh` — the full eighteen-corner ratified matrix
     (`spec/decision-records/0001-...` Decision 4), each corner an
     independent `klt place-and-route` (engine `openroad`) run against the
     **revised** floorplan: `floorplan.method` = `utilization`,
     `utilization_pct` **55** (was 35), `aspect_ratio` 1, `core_margin_um`
     **2** (was 4), `site` `unithd`; `io.layer_h`/`layer_v` = `met3`/`met2`
     (unchanged); `constraints.clock_port` = `clk`,
     `constraints.clock_period_ns` = 10.0 (100 MHz, unchanged); `seed` = 42
     (unchanged); `target_stage` = `route`. `flow/par-modexp.json` itself
     (the single-corner nominal recipe) is **left byte-identical** to the
     superseded record's version — its content hash is a live provenance
     input of two records this issue does not own
     (`verification/records/place-and-route/records/20260814-203901-c741877.md`
     and `verification/records/drc-lvs/records/20260815-013937-fa169a4.md`),
     so the revised floorplan lives only in `flow/run-corner-sweep.sh` (see
     that script's own "Floorplan note (issue #56)" comment and
     `flow/README.md`).
  4. One supplementary `report_checks -path_delay max` re-timing of the
     binding corner's routed database (`ss_n40C_1v28`), reproducing the
     superseded record's own critical-path-capture methodology (`read_db`
     the routed `.odb`, `read_liberty` the corner's deck, `create_clock`,
     `estimate_parasitics -global_routing`, then `report_checks`) — not
     part of `klt place-and-route`'s own response contract.
- **Statistical convention**: N/A (deterministic P&R runs at a pinned seed
  — the `ss_n40C_1v28` corner's route-stage metrics from this record's own
  sweep, `worst_slack_ns -30.5619`, reproduce byte-for-byte against an
  independent, standalone re-run of the identical request made before this
  record was minted).
- **Result**:
  - `stage_reached`: `route` for every one of the 18 corner runs.
  - Cell count (from routed `.def` `COMPONENTS`): **716** at the nominal
    corner (`tt_025C_1v80`, vs. 718 in the superseded record — 2 fewer
    post-route buffers, consistent with the smaller/denser floorplan) and
    **798** at the binding corner (`ss_n40C_1v28`, up from the nominal
    figure due to per-corner `repair_timing` inserting more fix-up buffers
    at the slow corner — per-corner cell count was not reported in the
    superseded record, so no direct prior comparison exists at this
    specific corner; die/core area (13422.4/12111.6 µm², both corners —
    floorplan sizing is corner-independent) is markedly smaller than the
    superseded record's 21969.2/19398.6 µm² at the same higher target
    utilization, as expected from the floorplan change itself.
  - Nominal corner (`tt_025C_1v80`): die 13422.4 µm², core 12111.6 µm²,
    reported utilization 59.23% (vs. 37.08% in the superseded record — same
    design, denser floorplan), routed wirelength 18401 µm, WNS **+3.334 ns**
    (100 MHz still closes, essentially unchanged from +3.318 ns), TNS 0,
    Fmax **150.02 MHz** (vs. 149.66 MHz), 0/0 violations, estimated power
    0.863 mW. The nominal corner was never the problem; this confirms the
    revised floorplan does not regress it.
  - **Full eighteen-corner sweep** (identical corner set to the superseded
    record): **100 MHz still closes at exactly the same 9/18 corners**
    (both `tt`, all seven `ff`) and **still fails at all nine `ss`
    corners** — the pass/fail partition by corner is unchanged. Per-corner
    Fmax/WNS/TNS/violation table:

    | Corner | WNS (ns) | TNS (ns) | Fmax (MHz) | Setup viol. | Hold viol. | Utilization | Power (mW) |
    | --- | --- | --- | --- | --- | --- | --- | --- |
    | `ff_100C_1v65` | 4.501 | 0.000 | 181.84 | 0 | 0 | 58.82% | 0.7102 |
    | `ff_100C_1v95` | 5.741 | 0.000 | 234.80 | 0 | 0 | 58.65% | 0.9992 |
    | `ff_n40C_1v56` | 3.659 | 0.000 | 157.70 | 0 | 0 | 59.50% | 0.6510 |
    | `ff_n40C_1v65` | 4.259 | 0.000 | 174.19 | 0 | 0 | 59.36% | 0.7311 |
    | `ff_n40C_1v76` | 4.638 | 0.000 | 186.49 | 0 | 0 | 58.81% | 0.7822 |
    | `ff_n40C_1v95` | 5.627 | 0.000 | 228.67 | 0 | 0 | 58.77% | 0.9638 |
    | `ff_n40C_1v95_ccsnoise` | 5.627 | 0.000 | 228.67 | 0 | 0 | 58.77% | 0.9638 |
    | `ss_100C_1v40` | **-3.891** | -60.575 | 71.99 | 16 | 0 | 63.47% | 0.5361 |
    | `ss_100C_1v60` | **-0.340** | -4.194 | 96.72 | 16 | 0 | 61.35% | 0.7022 |
    | `ss_n40C_1v28` | **-30.562** | -556.070 | 24.65 | 96 | 0 | 67.21% | 0.3075 |
    | `ss_n40C_1v35` | **-17.380** | -294.760 | 36.52 | 69 | 0 | 62.72% | 0.3940 |
    | `ss_n40C_1v40` | **-11.957** | -186.934 | 45.54 | 17 | 0 | 63.43% | 0.4673 |
    | `ss_n40C_1v44` | **-8.839** | -138.310 | 53.08 | 16 | 0 | 63.07% | 0.5247 |
    | `ss_n40C_1v60` | **-2.124** | -31.673 | 82.48 | 16 | 0 | 62.96% | 0.6811 |
    | `ss_n40C_1v60_ccsnoise` | **-2.124** | -31.673 | 82.48 | 16 | 0 | 62.96% | 0.6811 |
    | `ss_n40C_1v76` | **-0.189** | -0.979 | 98.15 | 9 | 0 | 59.70% | 0.8407 |
    | `tt_025C_1v80` | 3.334 | 0.000 | 150.02 | 0 | 0 | 59.23% | 0.8630 |
    | `tt_100C_1v80` | 3.524 | 0.000 | 154.41 | 0 | 0 | 58.99% | 0.8471 |

  - **Binding corner is still `ss_n40C_1v28`**, by a wide margin — no
    corner changed which side of 100 MHz it lands on, and `ss_n40C_1v28`
    remains far and away the worst by both WNS and Fmax. Measured worst-
    corner Fmax is **24.65 MHz** (23.9825 → 24.6537 MHz precisely), WNS
    **-30.562 ns** (was -31.697 ns), TNS -556.070 ns (was -566.638 ns), 96
    setup violations (was 98), 0 hold violations.
  - **How much of the gap this closes, stated explicitly (record `0002`
    Decision 1's stated-corner requirement):** at the binding corner
    `ss_n40C_1v28`, this mapping-only change closes **1.135 ns of the
    31.697 ns WNS shortfall — 3.6% of the gap — leaving 96.4% (30.562 ns)
    open**. Achieved Fmax improved 23.98 → 24.65 MHz, a **2.8% relative
    increase**, still **~4.06×** short of the 100 MHz target (was ~4.17×).
    This is **not** a claim that 100 MHz closes at this or any additional
    corner — it does not; the pass/fail partition across all 18 corners is
    byte-for-byte unchanged from the superseded record. Per-corner detail
    across the other eight `ss` corners is mixed but small: seven of the
    other eight improved by 0.05–0.45 ns of WNS (`ss_n40C_1v76` improved
    the most in absolute WNS terms, 0.641 → 0.189 ns, now closest to
    closing of any `ss` corner); `ss_n40C_1v60`/`ss_n40C_1v60_ccsnoise`
    each *regressed* slightly (WNS -2.039 → -2.124 ns, -0.085 ns) — the
    revised floorplan is not uniformly beneficial, consistent with a
    mapping change perturbing a fixed-depth logic chain's routing rather
    than shortening it.
  - **This confirms record `0002` Decision 3's structural framing**: the
    binding-corner critical path remains the identical flop pair
    (`_1205_` → endpoint through `rtl/modexp.v`'s `mm_sum`/`mm_red` chain,
    ending at the same `_1172_/D` sink observed in the superseded record)
    through the same `maj3`/`xnor2` full-adder-into-compare-and-subtract-
    mux-chain gate sequence — a mapping-only change moves cell placement
    and buffering but cannot shorten a fixed-depth combinational chain by
    more than a few percent. Fresh supplementary
    `report_checks -path_delay max` re-timing:
    `artifacts/20260816-072918-d8eafca/ss_n40C_1v28-critical-path.txt`
    (data arrival 38.86 ns vs. required 8.30 ns, slack -30.56 ns — matches
    this record's own route-stage WNS to within rounding).
  - **Conclusion for issue #56's own question** ("how much of the gap is
    mapping-attributable vs. architecture-bound"): the overwhelming majority
    (96.4% of the binding-corner WNS shortfall) is **architecture-bound**,
    not mapping-bound. The floorplan/cell-mapping knobs exposed by `klt
    place-and-route`'s request schema (floorplan utilization/aspect-
    ratio/margin, IO metal layer, seed) were explored beyond the single
    configuration in this table before selecting it — utilization 20%/35%/
    55% (each with a correspondingly adjusted `core_margin_um` of 8/4/2),
    an alternate aspect ratio (2:1 at 55% utilization), IO layer met4/met3
    and met5/met4, and an alternate seed were all tried and completed at
    `ss_n40C_1v28` during this issue's work (informal, not separately
    archived as evidence — the smallest WNS shortfall observed across that
    exploration was this record's chosen 55%/`core_margin_um` 2
    configuration, at -30.56 ns; every other configuration that completed
    was equal or worse; one (`utilization_pct` 35 with IO layer met4/met3)
    failed P&R entirely at the `place` stage, `PPL-0045`; a 75%-utilization
    attempt was started but not completed and is not counted as a data
    point either way). `global_placement`'s own density target and any
    per-cell
    drive-strength/`DONT_USE_CELLS` constraint are **not** exposed by the
    request schema (`klayout_tools/place_and_route.py`'s
    `_GLOBAL_PLACEMENT_DENSITY` is a fixed internal constant, not a request
    field), so this record's search covers the floorplan/mapping knob space
    `klt place-and-route` v1 actually exposes, not a hypothetical larger
    one. Per record `0002` Decision 3, the next step — if the program wants
    to close the remaining gap — is the named RTL restructuring of
    `mm_sum`/`mm_red`'s single-cycle add-then-reduce chain, which is
    explicitly **out of scope for issue #56** (its own decision record,
    latency-formula re-derivation, and full bit-exact re-verification).
  - **Overall: PASS** for `stage_reached: "route"` at all 18 corners, no RTL
    change (verified), and the fresh full-corner-matrix measurement this
    record's claim promises. It is **not** a claim of new 100 MHz closure
    at any corner beyond the same 9/18 the superseded record already
    reported — stated plainly per record `0002` Decision 1's requirement
    that no "closes 100 MHz" claim in this repository go unqualified.
- **klt provenance**: klt 0.2.0; OpenROAD `26Q3-1260-g06a5a02279` (pinned
  `openroad/orfs:26Q3-296-gda37dce1c` Docker image, digest
  `sha256:ebc8142da6d65d1a1e9a528aa2cedcde356243465dd859af8d3ade51075f8cb2`
  — unchanged from the superseded record); PDK: sky130A (open_pdks
  `c6d73a35f524070e85faff4a6a9eef49553ebc2b`, unchanged); deck (nominal
  corner) content_hash `sha256:8e78e14442062dba34d414fca6490b2f6b96038d4510d1438ca44fee31487135`
  (matches the superseded record exactly, confirming the PDK/deck
  themselves are unchanged); input (post-tie-cell netlist) content_hash
  `sha256:67a218e16cf51e4f3010e5f87404b829f0574d9765b2569278abd54a4fdc7486`
  (matches the superseded record exactly, confirming no RTL/synthesis
  change).
- **Links**:
  - Request recipes: `flow/synthesize-modexp.json` (unchanged),
    `flow/par-modexp.json` (unchanged — deliberately, see "Design
    provenance" above), `flow/run-corner-sweep.sh` (revised floorplan)
  - Superseded record: `verification/records/place-and-route/records/20260814-203901-c741877.md`
  - Decision record followed: `spec/decision-records/0002-slow-corner-timing-closure-and-mm-red-critical-path.md`
    (Decision 3's named first step)
  - Corner matrix ratified by: `spec/decision-records/0001-input-domain-interface-and-corner-matrix.md`
    Decision 4
  - Design under test: `rtl/modexp.v` (unmodified — `git diff` empty
    against the superseded record's revision)
  - Bit-exact correctness re-check on this record's own commit:
    `python3 verification/cross_check.py` — 2000/2000 match, 0 mismatches
    across `WIDTH` = 4, 6, 8, 16
  - Full 18-corner sweep raw output:
    `verification/records/place-and-route/artifacts/20260816-072918-d8eafca/corner-sweep-results.json`
  - Raw nominal-corner `klt place-and-route` JSON envelope:
    `verification/records/place-and-route/artifacts/20260816-072918-d8eafca/par-nominal-output.json`
  - Resolved per-corner request snapshots (nominal + binding corner):
    `verification/records/place-and-route/artifacts/20260816-072918-d8eafca/par-modexp-tt_025C_1v80.json`,
    `verification/records/place-and-route/artifacts/20260816-072918-d8eafca/par-modexp-ss_n40C_1v28.json`
  - Script snapshot (revised floorplan): `verification/records/place-and-route/artifacts/20260816-072918-d8eafca/run-corner-sweep.sh`
  - Netlist snapshot: `verification/records/place-and-route/artifacts/20260816-072918-d8eafca/modexp_synth_tied.v`
  - Supplementary critical-path re-timing: `verification/records/place-and-route/artifacts/20260816-072918-d8eafca/ss_n40C_1v28-critical-path.txt`
  - Follow-up: `docs/baseline.md#full-corner-matrix-sweep` updated to cite
    this record; any RTL-restructuring follow-up per record `0002`
    Decision 3 step 2 is a separate, not-yet-filed issue (explicitly out of
    scope here).
- **Timestamp / author**: 2026-08-16T07:29:18Z, loom-builder (issue #56)
- **Supersedes**: `20260814-203901-c741877` (freshness only — that record's
  own numbers, at the original 35%/`core_margin_um` 4 floorplan, remain
  correct history and are not deleted or edited; this record supersedes it
  because `flow/run-corner-sweep.sh`'s content hash changed and per
  `verification/check_records.py`'s provenance-hash-freshness check, the
  live record citing it must be the current one)
