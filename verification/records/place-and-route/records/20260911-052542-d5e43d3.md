<!-- record-meta
{
  "record_id": "20260911-052542-d5e43d3",
  "experiment": "place-and-route",
  "supersedes": "20260814-203901-c741877",
  "git_revision": "d5e43d3ba64ad967a9d35fd4c2907b4ef8df8961",
  "provenance": {
    "klt_version": "0.4.0",
    "pdk": {
      "name": "sky130A",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "content_hash": "sha256:8e78e14442062dba34d414fca6490b2f6b96038d4510d1438ca44fee31487135"
    },
    "inputs": [
      {
        "path": "rtl/modexp.v",
        "content_hash": "sha256:109db84055efe603af2e71506aa7630483696d84653b1de4c0c1d05171b8ed93"
      },
      {
        "path": "flow/tie_constants.py",
        "content_hash": "sha256:ed02a26d6ffc357bd36bc76461004797bf34d1b69bbb0fca5a3a873f92eb5b17"
      },
      {
        "path": "flow/synthesize-modexp.json",
        "content_hash": "sha256:6b86cf02f7aa159a07099f1b74b4cda1f494a568cda373958731bc279df44e30"
      },
      {
        "path": "flow/par-modexp.json",
        "content_hash": "sha256:3bc5503a74754b791fe898126dbafe47c9f7b9ecd926e17a78d3615a8c5390ef"
      }
    ]
  }
}
-->

# Record 20260911-052542-d5e43d3

- **Record ID**: 20260911-052542-d5e43d3
- **Claim**: issue #81 — `flow/par-modexp.json` gains a `power` block driving
  `klt place-and-route`'s `request.power` field (tapcell + PDN + filler-cell
  insertion, added upstream by `klayout-tools#1120`, merged 2026-08-17,
  already available at this repo's pinned `klt` revision per issue #81's own
  curation). This record measures the regenerated nominal-corner
  (`tt_025C_1v80`) `layout/modexp.def`/`layout/modexp.gds` this produces —
  the artifacts this repo signs off DRC/LVS against. **No RTL change**
  (`rtl/modexp.v` content hash unchanged from the superseded record) and
  **no floorplan change** (`floorplan` block of `flow/par-modexp.json`
  byte-identical to the superseded record's — only the new, additive
  `power` block differs, confirmed by diffing the two committed JSON
  documents). This record does **not** re-run the eighteen-corner ratified
  sweep (`spec/decision-records/0001-...` Decision 4) — that sweep's own
  most recent record
  (`verification/records/place-and-route/records/20260816-072918-d8eafca.md`)
  is a separate, still-current, unrelated evidence chain against issue #56's
  own revised (55% utilization) floorplan via `flow/run-corner-sweep.sh`,
  untouched by this issue; re-sweeping all 18 corners with `request.power`
  enabled is explicitly out of this issue's scope (filler-cell/tapcell/PDN
  closure for the single committed `layout/modexp.gds`, not a fresh
  corner-matrix study).
- **Design provenance**: netlist (post-synthesis, byte-identical to the
  superseded record —
  `flow/.klt/synthesize/modexp_synth_tied.v`, content_hash
  `sha256:67a218e16cf51e4f3010e5f87404b829f0574d9765b2569278abd54a4fdc7486`),
  derived from `rtl/modexp.v` @ `d5e43d3ba64ad967a9d35fd4c2907b4ef8df8961`
  (RTL itself unchanged since the superseded record — confirmed by the
  identical `rtl/modexp.v` and post-tie-cell-netlist content hashes above,
  and by `python3 verification/cross_check.py` passing at every verified
  `WIDTH` on this record's own commit, per `CLAUDE.md`'s correctness gate).
  The routed layout is the newly regenerated `layout/modexp.def` /
  `layout/modexp.gds`.
- **Run configuration**:
  1. `klt synthesize` (engine `yosys`) against `flow/synthesize-modexp.json`
     — unchanged from the superseded record (byte-identical output,
     verified by the matching netlist content hash above).
  2. `python3 flow/tie_constants.py` — unchanged.
  3. `klt place-and-route` (engine `openroad`) against `flow/par-modexp.json`
     — `floorplan` block byte-identical to the superseded record's
     (`utilization` method, 35%, `aspect_ratio` 1, `core_margin_um` 4, site
     `unithd`); `io.layer_h`/`layer_v` = `met3`/`met2` (unchanged);
     `constraints.clock_port` = `clk`, `constraints.clock_period_ns` = 10.0
     (100 MHz, unchanged); `seed` = 42 (unchanged); `target_stage` = `route`
     (unchanged); `pdk.corner` = `tt_025C_1v80` (nominal, unchanged). **New**
     — a `power` block: `power_net` `VDD`, `ground_net` `VSS`, `straps`
     `met1` (0.48 µm width, 5.44 µm pitch, `followpins: true` — row-rail
     stripe geometry) + `met4` (1.6 µm width, 27.14 µm pitch, 13.57 µm
     offset) + `met5` (1.6 µm width, 27.2 µm pitch, 13.6 µm offset), per
     `docs/cli/place-and-route.md`'s documented `sky130_fd_sc_hd` PDN
     example (klayout-tools' own tables, cross-checked by that project
     against OpenROAD-flow-scripts' `platforms/sky130hd/{config.mk,
     tapcell.tcl,pdn.tcl}` per klayout-tools#1120's own PR summary — not
     invented here). Re-run twice cold from this worktree (once producing
     the committed artifacts, once as an independent reproducibility check)
     — both runs report `status: "ok"`, `stage_reached: "route"`, and
     byte-identical `layout/modexp.def`/`layout/modexp.gds` content hashes,
     confirming the pinned `seed: 42` P&R run is deterministic at this
     `klt`/OpenROAD/PDK pin.
- **Statistical convention**: N/A (deterministic P&R run at a pinned seed —
  reproduced byte-for-byte across two independent cold runs, see above).
- **Result**:
  - `status`: `"ok"`, `stage_reached`: `"route"`.
  - `power` response field (test plan item 1): `pdn: true`, `global_connect:
    true`, `power_net: "VDD"`, `ground_net: "VSS"`, `tapcell_master:
    "sky130_fd_sc_hd__tapvpwrvgnd_1"`, `filler_masters: ["sky130_fd_sc_hd__fill_1",
    "sky130_fd_sc_hd__fill_2", "sky130_fd_sc_hd__fill_4",
    "sky130_fd_sc_hd__fill_8"]` — matches the test plan's acceptance
    criterion exactly.
  - Cell-count trajectory (from stage `-metrics` JSON dumps,
    `design__instance__count`): 683 (post-synthesis + tie cell, unchanged) →
    **948** (post-floorplan, +265 `sky130_fd_sc_hd__tapvpwrvgnd_1` tapcell
    instances — floorplan stage now runs `tapcell` per the new `power`
    block) → 951 (post-place, +3 `repair_design`/`repair_timing` fix-up
    buffers, down from the superseded record's +3 at this stage too) → 983
    (post-CTS, +32 clock-tree buffers) → 987 (post-route, +4
    `sky130_fd_sc_hd__diode_2` antenna fix-up cells) → **3074** (final,
    +2087 `sky130_fd_sc_hd__fill_{1,2,4,8}` filler-cell instances —
    `filler_placement` runs at the end of the `route` stage per the new
    `power` block). Net: **718 logic-bearing instances** (identical
    concept to the superseded record's own 718 — confirmed by direct
    instance-name/type accounting against the same, unchanged synthesis
    netlist: 680 present under an identical name and cell type, 3 resized
    [`_0565_` nor2_1→nor2_2, `_0785_` o22ai_1→o22ai_2, `_0913_`
    o21ai_0→o21ai_2], 35 new CTS/timing/antenna-fixup insertions, 0
    synthesis instances absent from the routed layout) **+ 2356
    physical-only instances new to this record** (265 tapcells + 2087
    fillers + 4 antenna diodes) that the superseded record's GDS had none
    of.
  - Nominal corner (`tt_025C_1v80`): die area **21969.2 µm²** (unchanged —
    same floorplan), core area **19398.6 µm²** (unchanged), reported
    utilization **39.31%** (up from 37.08% — same core area, more placed
    area now that tapcells/fillers occupy previously-empty row space),
    routed wirelength **19060 µm** (was 17805 µm), WNS **+3.736 ns** (100
    MHz still closes, with slightly more margin than the superseded
    record's +3.318 ns), TNS 0, achieved Fmax **159.63 MHz** (was
    149.66 MHz), 0 setup / 0 hold / 0 antenna / 0 route-DRC violations,
    estimated power **0.983 mW** (was 0.876 mW, expected — many more
    instances, plus real PDN strap capacitance now modeled). **These
    incidental Fmax/wirelength/power deltas are not a claim of anything
    beyond what they are — the floorplan is unchanged; the deltas are
    OpenROAD's own legalization/CTS/optimization responding to tapcell rows
    now occupying fixed placement sites and antenna fix-ups now running —
    not a cell-count or PPA comparison against any external work (see
    `CLAUDE.md`'s overclaim-trap note).**
  - Full eighteen-corner sweep: **not re-run** by this record (see "Claim"
    above) — the ratified corner matrix's current sweep-based evidence is
    unaffected and unchanged (`20260816-072918-d8eafca`, a separate
    floorplan).
  - GDS scope, updated: tapcell insertion, power-grid (PDN) generation, and
    filler-cell insertion are now present. **Still absent**: metal
    (density) fill (`request.power` drives standard-cell row-filler
    insertion, not an OpenROAD `add_fill`/density-fill pass over the
    routing layers) and `DONT_USE_CELLS` exclusion — both unrelated to
    this issue's `power` block and out of its scope.
  - **Overall: PASS** for `stage_reached: "route"`, the `power` response
    field's acceptance criterion, nominal-corner 100 MHz closure (still
    closes, with margin), and byte-for-byte P&R reproducibility across two
    independent cold runs.
- **klt provenance**: klt 0.4.0 @ `f77036bff1eaf97b992e121acd702a98519142fb`
  (`docs/environment.md`); OpenROAD `26Q3-1260-g06a5a02279` (pinned
  `openroad/orfs:26Q3-296-gda37dce1c` Docker image, digest
  `sha256:ebc8142da6d65d1a1e9a528aa2cedcde356243465dd859af8d3ade51075f8cb2`,
  unchanged); PDK: sky130A (open_pdks
  `c6d73a35f524070e85faff4a6a9eef49553ebc2b`, unchanged); deck (nominal
  corner) content_hash `sha256:8e78e14442062dba34d414fca6490b2f6b96038d4510d1438ca44fee31487135`
  (matches the superseded record exactly — same PDK/deck); input
  (post-tie-cell netlist) content_hash
  `sha256:67a218e16cf51e4f3010e5f87404b829f0574d9765b2569278abd54a4fdc7486`
  (matches the superseded record exactly — no RTL/synthesis change).
- **Links**:
  - Request recipe: `flow/par-modexp.json` (new `power` block; `floorplan`
    left byte-identical to the superseded record's, per issue #81's own
    implementation guidance)
  - Superseded record: `verification/records/place-and-route/records/20260814-203901-c741877.md`
  - Unrelated, still-current corner-sweep evidence (issue #56's own
    revised floorplan, not re-run here): `verification/records/place-and-route/records/20260816-072918-d8eafca.md`
  - Originating issue: [sky130-modexp#81](https://github.com/2AMLogic/sky130-modexp/issues/81)
    (itself following on [sky130-modexp#79](https://github.com/2AMLogic/sky130-modexp/issues/79))
  - Upstream capability this issue wires up:
    [klayout-tools#1120](https://github.com/2AMLogic/klayout-tools/pull/1120)
    ("feat(place-and-route): power delivery — tapcell/PDN/filler-cell
    insertion via `request.power`", merged 2026-08-17, closing
    [klayout-tools#1091](https://github.com/2AMLogic/klayout-tools/issues/1091))
  - Layout artifacts: `layout/modexp.def`, `layout/modexp.gds`,
    `layout/README.md` (provenance)
  - Raw `klt place-and-route` JSON envelope (nominal corner, cold re-run):
    `artifacts/20260911-052542-d5e43d3/par-nominal-output.json`
  - Request snapshot: `artifacts/20260911-052542-d5e43d3/par-modexp.json`
  - DRC/LVS re-run against this layout: `verification/records/drc-lvs/records/20260911-053500-d5e43d3.md`
    (and its twin, `20260911-053520-d5e43d3.md`)
- **Timestamp / author**: 2026-09-11T05:25:42Z, loom-builder (issue #81)
- **Supersedes**: 20260814-203901-c741877 (freshness only, for the
  single-corner nominal recipe `flow/par-modexp.json` drives — that
  record's own numbers, at the pre-`power`-block configuration, remain
  correct history and are not deleted or edited)
