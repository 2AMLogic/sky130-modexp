<!-- record-meta
{
  "record_id": "20260815-102628-d13cc14",
  "experiment": "gate-level-sim",
  "supersedes": "20260815-072838-564b598",
  "git_revision": "d13cc141652fee3fc6a2ca20903399ca21c0c991",
  "provenance": {
    "klt_version": "0.2.0",
    "pdk": {
      "name": "sky130A",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "n/a",
      "content_hash": "n/a",
      "note": "no DRC/extraction deck is involved in a simulation run; the extraction deck that produced this record's input netlist is recorded in verification/records/drc-lvs/ (sha256:ec2b16dc7fc43cec24782a47531e8aa744160d487e5c0523523db5d600ad9935)",
      "cell_models": [
        {
          "path": "<sky130A>/libs.ref/sky130_fd_sc_hd/verilog/sky130_fd_sc_hd.v",
          "content_hash": "sha256:12d5d6448ccf265c544d88402d5c10ab6de9bee8b8cde48eee1f31ea8c3c3e45"
        },
        {
          "path": "<sky130A>/libs.ref/sky130_fd_sc_hd/verilog/primitives.v",
          "content_hash": "sha256:7d4b1441d06b0f2b9662befa675a0484d77149e04eb42a5f1fa2d4cfded7b2ce"
        }
      ],
      "cell_model_note": "not under version control in this repo -- pinned transitively by pdk.version (open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b); resolve with `klt pdk find --pdk sky130A`. Hashes measured on the environment this record was produced on, so staleness is detectable by hand even though check_records.py cannot recompute a path outside the working tree. Identical to the superseded record's hashes -- same pinned PDK version, unchanged content.",
      "cell_model_defines": ["FUNCTIONAL", "UNIT_DELAY=#1"],
      "cell_model_use_power_pins": false
    },
    "inputs": [
      {
        "path": "layout/modexp.gds",
        "content_hash": "sha256:229ed6a5f92938699acc969f757d84b9348731bacbe12a485473161e917c8d10"
      },
      {
        "path": "layout/lvs/modexp_layout_abstracted.spice",
        "content_hash": "sha256:04748e40b81d7e623dde2ed380be0d61cc097e57b0ff21bcd312f3f0a0691e0b"
      },
      {
        "path": "layout/lvs/modexp_layout_extract_report.json",
        "content_hash": "sha256:2f7b948d83db7ed0e506e59bc86360b8a886a73244fd5c7f2a7b1eeedd8439bd"
      },
      {
        "path": "verification/gate-level/spice_to_verilog.py",
        "content_hash": "sha256:8a50d3b4fa283f637d2ed4d5f555df7edc41c1b25ec4d012d5c9b9bfefeccaa0"
      },
      {
        "path": "verification/gate-level/modexp_post_route.v",
        "content_hash": "sha256:2551df0bcdb55c12d2d86dfbc62d7c3f40ad87856ce8b9b08dc5c2249bf13206"
      },
      {
        "path": "verification/gate-level/sky130_fd_sc_hd_sim_defines.v",
        "content_hash": "sha256:77dea4c4aa8230209c29f1157f136e9f529ec8536bceec917e6cdc065806a9cb"
      },
      {
        "path": "verification/gate-level/request-modexp-gate-level.json",
        "content_hash": "sha256:52889553cb8a7fd452d05d65fe237c51a0380a3d92eb4d69b6bbb6f9df2b22c9"
      },
      {
        "path": "verification/gate-level/gate_level_cross_check.py",
        "content_hash": "sha256:c2f0ecf4a2f8ffdd705772d61392d6c761b549b30ef9e47488d42d3b47bb8a1a"
      },
      {
        "path": "verification/test_modexp.py",
        "content_hash": "sha256:29d66b2c723b823cb8b9ee1cf2565bde9a4fb59f92a587a40ecb01a3edc498dd"
      },
      {
        "path": "verification/cross_check_tb.py",
        "content_hash": "sha256:34bb7a2b032eefd664a2e75c04ff88e457000e9391e6c6d1a55cffa5d59cea9e"
      }
    ]
  }
}
-->

# Record 20260815-102628-d13cc14

- **Record ID**: 20260815-102628-d13cc14
- **Claim**: `spec/modexp.md`'s Correctness row (*"bit-exact vs `pow(b, e,
  m)`, randomized, on every commit"*) re-established against the **routed
  layout** rather than the RTL, and `docs/baseline.md#post-route-gate-level-simulation`.
  Substantiates issue #9's Leg 1 (zero-delay gate-level equivalence).
  **Does not** substantiate #9's Leg 2 (delay-annotated simulation at the
  corner set), same as the record this one supersedes.
- **Design provenance**: layout (post-P&R, extracted) — identical chain and
  netlist to the superseded record (`layout/modexp.gds`'s content hash is
  unchanged: `sha256:229ed6a5...e917c8d10`), re-derived here to confirm no
  drift: `layout/modexp.gds` → (issue #8's `klt extract --abstract-cells`)
  `layout/lvs/modexp_layout_abstracted.spice` →
  (`verification/gate-level/spice_to_verilog.py`)
  `verification/gate-level/modexp_post_route.v`. **718 instances / 59
  distinct cell types / 68 top-level pins / 770 signal + 444 power-pin nets
  = 1214 nets**, cross-checked against
  `layout/lvs/modexp_layout_extract_report.json` before the run was allowed
  to proceed — same figures as the superseded record, byte-identical
  regenerated netlist (`modexp_post_route.v` content hash unchanged).
- **Run configuration**:
  1. `./verification/gate-level/run-gate-level-sim.sh` — regenerates the
     netlist (with `--check`), links the resolved PDK cell models, then
     runs `klt functional-verification
     verification/gate-level/request-modexp-gate-level.json --format json`.
     Engine `icarus` 13.0, cocotb 2.0.1, timescale `1ns/1ps`,
     `random_seed: 1`. Testbench `test_modexp` — the committed
     `verification/test_modexp.py`, **unmodified**, reached through a git
     symlink (`verification/gate-level/test_modexp.py`, mode `120000`), not
     a copy.
  2. `python3 verification/gate-level/gate_level_cross_check.py
     --compare-rtl verification/records/width-cross-check/artifacts/20260815-073109-564b598/width-16.jsonl`
     — 500 randomized cases driving `verification/cross_check_tb.py`
     unmodified through cocotb's own `Runner` (same reasoning as the
     superseded record; see "klt provenance").
  - **Fix required to re-run this experiment, not present at the superseded
    record's time**: `verification/test_modexp.py` (since PR #27, issue #23)
    locates its sibling `_dut.py` via `sys.path.insert(0,
    os.path.dirname(os.path.abspath(__file__)))`. Python does not resolve a
    symlink when computing `__file__`, so when `test_modexp.py` is reached
    *through* `verification/gate-level/test_modexp.py` (a symlink), that
    computed directory is `verification/gate-level/`, not `verification/` —
    and `_dut.py` did not exist there. Leg 1a's `klt
    functional-verification` run therefore failed
    (`ModuleNotFoundError: No module named '_dut'`) until a matching git
    symlink, `verification/gate-level/_dut.py -> ../_dut.py`, was added
    (this record's only source change, alongside itself). Leg 1b was
    unaffected: `gate_level_cross_check.py` invokes `cross_check_tb.py`
    directly (not through a symlink) with `PYTHONPATH` set to
    `verification/`, so `_dut.py` was already resolvable there.
  - Cell models: `<sky130A>/libs.ref/sky130_fd_sc_hd/verilog/sky130_fd_sc_hd.v`
    + `primitives.v`, compiled with `` `define FUNCTIONAL `` and
    `` `define UNIT_DELAY #1 ``, **without** `USE_POWER_PINS` — unchanged
    from the superseded record (identical content hashes).
  - **Corner: none** — same reasoning as the superseded record (a zero-delay
    functional run has no corner attribute; corner dependence enters only
    through SDF, which is the blocked Leg 2).
  - **Environment note**: this run's `iverilog` resolved to 13.0 (this
    repo's general pin, `docs/environment.md`), not the 12.0 the superseded
    record used — an unrelated environment difference (a different host),
    not a re-pin. Leg 1a/1b results are unaffected (see Result). This does
    **not** touch Leg 2's blocker (B), which is specific to Icarus 12.0 and
    is carried forward unchanged (see Result below).
- **Statistical convention**: two independent legs, unchanged from the
  superseded record. Leg 1a is deterministic (7 directed vectors + 40
  randomized at `random_seed: 1`, exactly what the committed
  `test_modexp.py` contains). Leg 1b is N = 500 randomized cases at
  `WIDTH=16`, drawn from `random.Random(20260807016)` — the same pinned
  seed derivation `verification/cross_check.py` uses, compared here against
  the current live RTL transcript
  `verification/records/width-cross-check/records/20260815-073109-564b598.md`
  (itself a same-cause re-mint: PR #27's `_dut.py` extraction). 0-mismatch
  pass/fail, not a distribution claim.
- **Result**:
  - **Leg 1a — `klt functional-verification`, unmodified `test_modexp.py`:
    PASS.** `status: "pass"`, `test_count: 2`, `passed_count: 2`,
    `failed_count: 0`. Simulated time is **identical** to both the RTL
    run's and the superseded record's (`test_modexp_known_vectors` 23750.0
    ns, `test_modexp_random` 180480.0 ns) — the routed design remains
    cycle-for-cycle identical to the RTL after the `_dut.py` refactor, not
    merely functionally equivalent.
  - **Leg 1b — 500-case deep run: PASS**, 500/500 match, 0 mismatches — and
    the per-vector JSONL transcript is **byte-identical** to the current
    live RTL transcript
    `verification/records/width-cross-check/artifacts/20260815-073109-564b598/width-16.jsonl`.
  - **Netlist provenance guards: all clean, unchanged.** 718 instances (not
    683+1), 59 cell types and 68 pins matching the extraction report
    exactly, every net present in the report's `nets[]`, zero
    multiply-driven nets, zero floating cell inputs.
  - **`test_modexp.py` / `cross_check_tb.py` unchanged in behavior**: their
    assertions and reference model (`pow(base, exp, mod)`) are unchanged;
    only their internal `reset`/`run_modexp` drive logic moved to the
    shared `verification/_dut.py` (issue #23 / PR #27), which is why this
    record exists — the prior record's pinned `content_hash` for both files
    went stale when that refactor landed on `main` after the prior record
    was minted. This record's `provenance.inputs` hashes match the current
    working tree (verified by `verification/check_records.py`).
  - **WIDTH set reduced to 16 only, same reasoning as the superseded
    record**: the netlist is an extraction of one fixed physical layout of
    one elaboration; `WIDTH` does not survive synthesis or P&R.
  - **Leg 2 — delay-annotated simulation at the corner set: still NOT
    ACHIEVED, blocked on upstream tooling — carried forward unchanged.**
    This record's cause for existing (a stale `provenance.inputs` hash on
    two Leg-1 testbench files) has no bearing on Leg 2's blockers, so Leg
    2's evidence is not re-captured here; it is referenced from the
    superseded record's own frozen artifact,
    `verification/records/gate-level-sim/artifacts/20260815-072838-564b598/leg2-blocker-evidence.txt`,
    which remains accurate:
    1. **No artifact carrying post-route delays exists** — `klt
       place-and-route`'s response contract is `def_path` + `gds_path`
       only, and `klt functional-verification`'s request schema has no SDF
       option. Simulator-independent; filed upstream as
       [klayout-tools#1002](https://github.com/2AMLogic/klayout-tools/issues/1002)
       (open).
    2. **Icarus 12.0 cannot run the annotatable models** (`-ginterconnect`
       unsupported; `-gspecify` alone drives the whole design to `x`) — this
       was measured specifically on Icarus 12.0, the version the superseded
       record's host resolved. It is **not** re-verified against this
       record's Icarus 13.0, since blocker (1) alone is already sufficient
       to block Leg 2 and re-litigating blocker (2) on a newer Icarus is
       out of scope for this record's stale-hash fix. Filed upstream as
       [klayout-tools#1004](https://github.com/2AMLogic/klayout-tools/issues/1004).
  - **No mismatch or timing failure was observed anywhere in the legs that
    ran**, so there is no failing vector to report.
  - **What this run does not model** — unchanged from the superseded
    record: no parasitic extraction, no timing beyond the race-avoidance
    `UNIT_DELAY`, no corner, no power/ground network, cell-instance
    granularity (not transistor level), and not an independent check of the
    extraction itself.
- **klt provenance**: `klt` 0.2.0 at this repo's pinned revision
  `af5791b557fc7c669c3981335a294256ccf37e6f` (`docs/environment.md`) drove
  Leg 1a end to end (`klt functional-verification`) and resolved the PDK
  (`klt pdk find`). PDK: sky130A, `open_pdks
  c6d73a35f524070e85faff4a6a9eef49553ebc2b` — same pinned version and same
  measured cell-model content hashes as the superseded record. No
  DRC/extraction deck is involved in a simulation run, so `deck.content_hash`
  is `n/a`; the extraction deck that produced this record's *input* netlist
  is recorded in `verification/records/drc-lvs/`
  (`sha256:ec2b16dc7fc43cec24782a47531e8aa744160d487e5c0523523db5d600ad9935`),
  and `layout/modexp.gds`'s content hash here is identical to the one that
  record cites. Leg 1b bypasses `klt` for elaboration (informational, same
  situation as the superseded record). Simulator: Icarus Verilog 13.0,
  cocotb 2.0.1 (see "Run configuration" for the version-note vs. the
  superseded record's host).
- **Links**:
  - Method, scope, and cell-model resolution: `verification/gate-level/README.md`
  - Converter: `verification/gate-level/spice_to_verilog.py`; its self-test:
    `verification/gate-level/test_spice_to_verilog.py` (run by `npm run lint`)
  - Runner: `verification/gate-level/run-gate-level-sim.sh`; request:
    `verification/gate-level/request-modexp-gate-level.json`
  - Deep-run driver: `verification/gate-level/gate_level_cross_check.py`
  - Netlist under test: `verification/gate-level/modexp_post_route.v`
  - Testbenches (both unmodified in behavior; drive logic shared via
    `verification/_dut.py`): `verification/test_modexp.py`,
    `verification/cross_check_tb.py`
  - New in this record: `verification/gate-level/_dut.py` (git symlink to
    `../_dut.py`, mode `120000`) — required for Leg 1a to resolve the
    testbench's shared drive-logic import through the `test_modexp.py`
    symlink; see "Run configuration".
  - Layout under test: `layout/modexp.gds`, `layout/lvs/README.md`
  - Upstream record this one builds on:
    `verification/records/drc-lvs/records/20260815-013937-fa169a4.md` (the
    extraction), `verification/records/width-cross-check/records/20260815-073109-564b598.md`
    (the current RTL transcript compared against)
  - Superseded record (frozen history, including Leg 2 blocker evidence,
    still accurate — see Result):
    `verification/records/gate-level-sim/records/20260815-072838-564b598.md`
  - Frozen artifacts:
    `artifacts/20260815-102628-d13cc14/klt-functional-verification.json`,
    `artifacts/20260815-102628-d13cc14/run-console.txt`,
    `artifacts/20260815-102628-d13cc14/gate-level-width-16.jsonl`,
    `artifacts/20260815-102628-d13cc14/cross-check-console.txt`,
    `artifacts/20260815-102628-d13cc14/modexp_post_route.v`
  - Friction filed (unchanged, none new — see Result's Leg 2 note):
    [klayout-tools#1001](https://github.com/2AMLogic/klayout-tools/issues/1001),
    [#1002](https://github.com/2AMLogic/klayout-tools/issues/1002),
    [#1003](https://github.com/2AMLogic/klayout-tools/issues/1003),
    [#1004](https://github.com/2AMLogic/klayout-tools/issues/1004);
    previously [#996](https://github.com/2AMLogic/klayout-tools/issues/996)
- **Timestamp / author**: 2026-08-15T10:26:28Z, loom-builder (issue #35)
- **Supersedes**: `20260815-072838-564b598` -- PR #27 (issue #23) extracted
  `test_modexp.py`'s and `cross_check_tb.py`'s shared `reset`/`run_modexp`
  drive logic into `verification/_dut.py` and merged to `main` first; PR #26
  (this experiment's original record) had branched before that refactor and
  merged second, so its committed record embedded pre-refactor
  `content_hash` values for both files against the post-refactor tree —
  caught by `verification/check_records.py`'s freshness check (issue #35).
  This record re-runs the identical Leg 1 claim against the refactored
  testbenches (behavior unchanged, confirmed byte-identical simulated times
  and transcripts) and reconfirms PASS; it also fixes the `_dut.py`
  resolution gap the refactor introduced for the symlinked `test_modexp.py`
  (see "Run configuration"), which the superseded record's recipe could not
  have hit at the time it was produced. Leg 2's status and evidence are
  unaffected and carried forward by reference, not restated as new.
