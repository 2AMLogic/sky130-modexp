<!-- record-meta
{
  "record_id": "20260909-230216-92e00f2",
  "experiment": "gate-level-sim",
  "supersedes": "20260816-174310-5e656e5",
  "git_revision": "92e00f2b986d0f19d37722e159369ae6c42c53a5",
  "provenance": {
    "klt_version": "0.4.0+gf77036bff1ea",
    "klt_git_revision": "f77036bff1eaf97b992e121acd702a98519142fb",
    "pdk": {
      "name": "sky130A",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "content_hash": "sha256:5afac7ab8561545859f5e2e74f4621c6ffc052756dc8fe344ea263398e96b240"
    },
    "inputs": [
      {
        "path": "verification/records/place-and-route/artifacts/20260814-203901-c741877/modexp_synth_tied.v",
        "content_hash": "sha256:67a218e16cf51e4f3010e5f87404b829f0574d9765b2569278abd54a4fdc7486"
      },
      {
        "path": "verification/test_modexp.py",
        "content_hash": "sha256:d5993783035bad3f8299a5e378c9e8334e4e4cedd86bb86e23a8fe0bb29e4acd"
      }
    ]
  }
}
-->

# Record 20260909-230216-92e00f2

- **Record ID**: 20260909-230216-92e00f2
- **Claim**: `verification/gate-level/README.md`'s "Leg 2" section, re-attempted
  after issue #78 bumped `docs/environment.md`'s `klt` pin past
  [klayout-tools#1069](https://github.com/2AMLogic/klayout-tools/pull/1069)
  ("fix(functional-verification): resolve top-level-port SDF `INTERCONNECT`
  entries via a generated pass-through wrapper", merged
  2026-08-17T00:05:03Z at `ee10a5415f14d2a6a2f516355b276673e1700170`) — the
  fix for [klayout-tools#1056](https://github.com/2AMLogic/klayout-tools/issues/1056),
  which caused the superseded record's FAIL. **Live pin verification**:
  `gh api repos/2AMLogic/klayout-tools/compare/ee10a5415f14d2a6a2f516355b276673e1700170...f77036bff1eaf97b992e121acd702a98519142fb`
  → `{"status": "ahead", "ahead_by": 424, "behind_by": 0}` — the new pin is a
  strict descendant of the fix commit.
- **Design provenance**: netlist (post-route, as-built) — a fresh
  `klt place-and-route` re-run against the same frozen, previously-committed
  post-synthesis netlist
  (`verification/records/place-and-route/artifacts/20260814-203901-c741877/modexp_synth_tied.v`)
  and the same floorplan/seed as the superseded record (`utilization_pct:
  35`, `core_margin_um: 4`, `seed: 42`, `tt_025C_1v80`), with the same
  `openroad` engine (`26Q3-1260-g06a5a02279`, the `openroad/orfs:26Q3-296-
  gda37dce1c` image `docs/environment.md` pins — this host also has a newer
  `openroad` wrapper resolving `openroad/orfs:latest` on `$PATH` from a
  separate, unrelated local install; that wrapper was deliberately shadowed
  for this run by prepending a `PATH` entry pointing at this repo's own
  `scripts/openroad-docker.sh`, so `engine_version` in `par-output.json`
  stays comparable to the superseded record's). Only `klt` itself changed
  between this run and the superseded record's fresh P&R re-run — confirmed
  by the identical `provenance.inputs[].content_hash` values above (the
  frozen synthesis netlist and the unmodified testbench are byte-identical
  to what the superseded record cites).
- **Run configuration**: mirrors the superseded record's, `klt` revision
  only changed.
  1. `klt place-and-route` with `post_route_spef: true, post_route_sdf:
     true` added to a scratch copy of `flow/par-modexp.json`'s request
     (`flow/par-modexp.json` itself stays byte-identical — it is a live
     provenance input of records this issue does not own). Response:
     `status: "ok"`, `stage_reached: "route"`, `engine_version:
     "26Q3-1260-g06a5a02279"`, `verilog_path` (`write_verilog`) and
     `spef_sta.sdf_path` (`write_sdf`) populated, from the same OpenSTA
     session.
  2. A defines file (`sky130_fd_sc_hd_sdf_defines.v`, this record's own
     scratch copy) leaving `FUNCTIONAL` **undefined** (the inverse of Leg
     1's `sky130_fd_sc_hd_sim_defines.v`), since `options.sdf` requires the
     cell models' `specify`-block timing branch. `USE_POWER_PINS` stays
     undefined, matching Leg 1.
  3. `klt functional-verification` request: `sources`:
     `[sky130_fd_sc_hd_sdf_defines.v, primitives.v, sky130_fd_sc_hd.v,
     modexp_post_route_sdf.v]`, `hdl_toplevel: "modexp"`,
     `testbench.module: "test_modexp"` (the same, unmodified
     `verification/test_modexp.py`), `options.sdf: {"file":
     ".../modexp_route.sdf", "corner": "min"}` (`"min"` for the same
     reason the superseded record used it — this `write_sdf` output's
     header carries only min/max values, no typ).
  4. Icarus build args: `-gspecify -ginterconnect -s klt_sdf_annotate`
     (`klt`'s fixed `SDF_BUILD_ARGS`), plus `-s modexp` for the toplevel.
     At this `klt` revision, `klt functional-verification` additionally
     generates `klt_sdf_dut_wrapper.v` (klayout-tools#1069's fix: a
     transparent pass-through module wrapping the DUT so
     `$sdf_annotate`'s elaboration root is one level below the true
     top-level ports) and elaborates it as the `-s` root instead of
     `modexp` directly — both frozen in this record's `artifacts/`.
- **Statistical convention**: N/A (a single deterministic build+annotate+run
  against fixed inputs).
- **Result**: **Leg 2 RE-ATTEMPTED — still FAIL, but a materially different
  and narrower failure than the superseded record's.**

  **klayout-tools#1069's fix is confirmed working for its stated scope.**
  The superseded record described its 200 unresolved entries as "~753
  `INTERCONNECT` entries whose first token is a top-level port name" — that
  figure does not reconcile against this run's frozen SDF (below) and is
  not repeated here; this record states only what is independently counted
  directly from its own artifacts, not carried forward from the superseded
  record's arithmetic. Directly counted in this run's `modexp_route.sdf`
  (1,927 total `INTERCONNECT` entries): **68** entries have a top-level
  port (`base_in[N]`, `exp_in[N]`, `mod_in[N]`, `result[N]`, `done`) as
  either endpoint. Of those 68, **65 resolve cleanly** — including the
  `done` net's entry (line 1720, `_1233_.Q` → `done`), which the superseded
  record's own 200-entry failure set had included. Only **3** of the 68
  top-level-port-touching entries are among this run's 49 residual
  failures (the `mod_in[1]/[4]/[6]` → `ANTENNA_<N>.DIODE` entries, detailed
  below) — i.e. the fix resolves top-level-port `INTERCONNECT` entries
  essentially completely (65/68, 96%), and the fix's own stated scope
  (top-level-port entries generally, not specifically antenna/filler-cell
  loads) is confirmed working.

  **klt's own verdict** (`sdf-sim-response.json`, `artifacts/`): request
  error class `SDF back-annotation ... did not fully apply`, **49**
  diagnostics in the Icarus transcript (down from the superseded record's
  200) — `klt`'s SDF-diagnostic gate correctly still fails the run, since
  any unresolved entry fails it by design.
  - **130 `SDF WARNING: ... TIMINGCHECK not supported`** — unchanged,
    pre-known, exempt class (every cell's setup/hold check section, not
    implemented by Icarus).
  - **49 `SDF ERROR: ... Could not find intermodpath!`** — a **new,
    narrower** residual class, found only once #1069's fix made this
    attempt reach this far:
    - 3 are top-level-port `INTERCONNECT` entries whose target is an
      `ANTENNA_<N>.DIODE` filler-cell instance (`mod_in[1]`, `mod_in[4]`,
      `mod_in[6]`, each to their own antenna diode) — in each case, that
      same `mod_in[N]` net's *other*, earlier-listed `INTERCONNECT` entry
      (to a real standard-cell input pin) resolves without error.
    - 46 are purely internal instance-pin-to-instance-pin entries, all
      driven by one contiguous block of flip-flop instances
      (`_1153_.Q` through `_1168_.Q`). For every one of these driver
      nets, the **first**-listed `INTERCONNECT` entry for that net
      resolves cleanly (in most cases, to a `result[N]` top-level port);
      every **subsequent** listed entry for the same net (additional
      internal fanout loads) fails. This "only the first-listed
      `INTERCONNECT` entry per driver resolves" pattern does **not**
      hold universally across the SDF (e.g. `rst_n`'s many fanout
      entries all resolve, first and later alike), so it is not simply
      "second and later entries never resolve" — it is scoped to this
      specific set of ~16-17 driver nets, not root-caused further here
      per this issue's scope.
    - Every one of the 49 failing entries carries `(0.000:0.000:0.000)`
      in the SDF itself (zero delay either way), the same pattern the
      superseded record noted for its 200.
  - Independently corroborated: `results_icarus.xml.txt` (`artifacts/`) shows
    the regression **ran to completion** this time (unlike the superseded
    record's "did not run to completion" framing was about the gate, not
    the simulator — same as before, the simulator itself always ran to
    completion; what's new is `klt` now produces a results file even
    though it still reports the run failed via its own diagnostic gate).

  **The regression's own values still corroborate a real problem,
  unchanged from the superseded record**: both the directed test
  (`test_modexp_known_vectors`, `modexp(2,10,1000)`) and the randomized
  test (`test_modexp_random`, `modexp(25247,55125,55342)`) report `got 0`
  — the same uniform, constant-zero result on every case as the superseded
  record, even though `done`'s own `INTERCONNECT` entry now resolves
  cleanly. This means the constant-zero symptom is **not** explained by
  the previously-suspected `done`-unresolved theory; per this issue's
  scope, this new observation is recorded here, not root-caused further.
- **klt provenance**: `klt` 0.4.0+gf77036bff1ea @
  `f77036bff1eaf97b992e121acd702a98519142fb` for both the
  `place-and-route` (SPEF/SDF export) and `functional-verification` (SDF
  back-annotation) steps. Icarus Verilog 13.0 (stable), unchanged from the
  superseded record. PDK: sky130A (`c6d73a35f524070e85faff4a6a9eef49553ebc2b`),
  unchanged.
- **Links**:
  - Superseded record: `verification/records/gate-level-sim/records/20260816-174310-5e656e5.md`
  - Claim document: `verification/gate-level/README.md` ("Leg 2" section,
    updated by this issue)
  - Fixed upstream: [klayout-tools#1056](https://github.com/2AMLogic/klayout-tools/issues/1056)
    (closed), fixed by [klayout-tools#1069](https://github.com/2AMLogic/klayout-tools/pull/1069)
    (merged)
  - New finding, filed generically (no design-specific detail) per
    `CLAUDE.md`'s friction protocol as
    [klayout-tools#1619](https://github.com/2AMLogic/klayout-tools/issues/1619):
    a residual class of `INTERCONNECT` entries that still fail Icarus
    `$sdf_annotate` after the top-level-port wrapper fix, when a single
    driver net fans out to both a top-level port and one or more purely
    internal instance-pin loads (the port entry resolves; its sibling
    internal entries on the same net do not).
  - Raw artifacts (frozen at record time):
    `artifacts/20260909-230216-92e00f2/modexp_post_route_sdf.v`,
    `artifacts/20260909-230216-92e00f2/modexp_route.sdf`,
    `artifacts/20260909-230216-92e00f2/request-modexp-gate-level-sdf.json`,
    `artifacts/20260909-230216-92e00f2/sdf-sim-response.json`,
    `artifacts/20260909-230216-92e00f2/test_icarus.log.txt` (`.txt` suffix
    to bypass `.gitignore`'s `*.log` scratch-log pattern, matching the
    intent -- not the letter -- of that rule: this file is committed
    evidence, not a regenerable scratch log),
    `artifacts/20260909-230216-92e00f2/results_icarus.xml.txt` (same
    `.gitignore` note, for `results_icarus.xml`),
    `artifacts/20260909-230216-92e00f2/par-output.json`,
    `artifacts/20260909-230216-92e00f2/sky130_fd_sc_hd_sdf_defines.v`,
    `artifacts/20260909-230216-92e00f2/klt_sdf_dut_wrapper.v`
- **Timestamp / author**: 2026-09-09T23:02:16Z, loom-builder (issue #78)
- **Supersedes**: 20260816-174310-5e656e5
