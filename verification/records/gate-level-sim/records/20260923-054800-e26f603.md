<!-- record-meta
{
  "record_id": "20260923-054800-e26f603",
  "experiment": "gate-level-sim",
  "supersedes": "20260911-071039-ce2b24c",
  "git_revision": "e26f603f6dcf6a7c2603167c44e74588e603bb5d",
  "provenance": {
    "klt_version": "0.6.0+gdac2b5daceb6",
    "klt_git_revision": "dac2b5daceb69a2068d9d2ee190d7afe37b29af7",
    "pdk": {
      "name": "sky130A",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "content_hash": "sha256:db951a2b964d535739b0c3fa6ad92cf1514beeeb3f167ff55e7c2991158a0da9"
    },
    "inputs": [
      {
        "path": "verification/records/place-and-route/artifacts/20260814-203901-c741877/modexp_synth_tied.v",
        "content_hash": "sha256:67a218e16cf51e4f3010e5f87404b829f0574d9765b2569278abd54a4fdc7486"
      },
      {
        "path": "verification/test_modexp.py",
        "content_hash": "sha256:d5993783035bad3f8299a5e378c9e8334e4e4cedd86bb86e23a8fe0bb29e4acd"
      },
      {
        "path": "verification/_dut.py",
        "content_hash": "sha256:5d18ef3b38b7f27644d83d6f85000317d9ebe044ebdd7224c6881ffee57eb252"
      },
      {
        "path": "verification/records/gate-level-sim/artifacts/20260923-054800-e26f603/modexp_post_route_sdf.v",
        "content_hash": "sha256:2c75d0cd36dd1520cd07a8b37d5dbc2a10e7b664b1bdbe96bf44872ce2d8c999"
      },
      {
        "path": "verification/records/gate-level-sim/artifacts/20260923-054800-e26f603/modexp_route.raw.sdf",
        "content_hash": "sha256:11441fd645471ce5bfa9f66cf421059f9562ca97f791eb8ac92687f04449f1ec"
      },
      {
        "path": "verification/records/gate-level-sim/artifacts/20260923-054800-e26f603/modexp_route.sdf",
        "content_hash": "sha256:832d554c6b0f9ed3e8d1df806d3d73a04cda85eb31adc757c920afd1db2434c8"
      },
      {
        "path": "verification/records/gate-level-sim/artifacts/20260923-054800-e26f603/sky130_fd_sc_hd_sdf_defines.v",
        "content_hash": "sha256:db951a2b964d535739b0c3fa6ad92cf1514beeeb3f167ff55e7c2991158a0da9"
      },
      {
        "path": "verification/records/gate-level-sim/artifacts/20260923-054800-e26f603/request-modexp-gate-level-sdf.json",
        "content_hash": "sha256:dbc4961756737fcbb0ddcc3b435f29a953c1ffedb0739a877b66ac5096313e42"
      },
      {
        "path": "verification/records/gate-level-sim/artifacts/20260923-054800-e26f603/klt-functional-verification.json",
        "content_hash": "sha256:75a60b3bca04a96122f17585f5810a06b5aeea80b5631d769c0ea0839d0c3f88"
      }
    ]
  }
}
-->

# Record 20260923-054800-e26f603

- **Record ID**: 20260923-054800-e26f603
- **Claim**: `verification/gate-level/README.md`'s "Leg 2" section, **achieved as
  net-delay (INTERCONNECT-only) SDF back-annotation** — issue #133. The
  committed, unmodified bit-exact suite (`verification/test_modexp.py`) was
  re-run by `klt functional-verification` with `options.sdf` set against the
  as-built post-route gate-level netlist of a fresh `klt place-and-route` run
  of the current post-route flow, and the committed report carries
  **`environment.sdf.annotated: true`** — T1 item 7's machine-checkable
  field, which the block manifest's item 7 now cites (`klt signoff` grades
  it `met`). Result: `status: "pass"`, 2/2 tests, 0 failed, reproduced 3×
  byte-identically (determinism confirmed, not assumed). The annotation is
  **partial by construction and this record names exactly what is not
  annotated and why**: (a) every `IOPATH` (cell-internal arc) entry is
  excluded — including one on a timing-closed design, IOPATH back-annotation
  on the sky130 `specify`-branch models under Icarus 13 mechanically kills
  the simulation (isolation table below; filed generically as
  [klayout-tools#2364](https://github.com/2AMLogic/klayout-tools/issues/2364));
  (b) 4 `INTERCONNECT` entries onto antenna-diode filler loads are excluded
  — each carries `(0.000:0.000:0.000)` and targets a timing-empty
  physical-only cell (filed generically as
  [klayout-tools#2363](https://github.com/2AMLogic/klayout-tools/issues/2363));
  (c) all 130 `TIMINGCHECK` sections are dropped by Icarus — the
  pre-known exempt class `klt` itself reports as
  `environment.sdf.dropped.timingcheck`. What **is** annotated: all 1,924
  remaining `INTERCONNECT` entries — the routed design's net delays, the
  post-layout-specific content a zero-delay run cannot see — with **zero**
  unresolved annotations in the transcript.
- **Design provenance**: a fresh, self-consistent `klt place-and-route`
  re-run at the **current** post-route flow — a scratch copy of
  `flow/par-modexp.json` **including the `power` block issue #81 added**
  (tapcell + PDN + filler-cell insertion, the request that generated the
  current `layout/modexp.def`/`modexp.gds`) with the netlist repointed at
  the frozen post-synthesis netlist
  (`verification/records/place-and-route/artifacts/20260814-203901-c741877/modexp_synth_tied.v`,
  byte-identical to every Leg 2 attempt since 2026-08-16) and
  `post_route_spef: true, post_route_sdf: true` added. Response:
  `status: "ok"`, `stage_reached: "route"`, `engine_version:
  "26Q3-1260-g06a5a02279"` (the pinned `openroad/orfs:26Q3-296-gda37dce1c`
  Docker route — this host's newer `openroad` on `$PATH` was shadowed via a
  `PATH` shim to this repo's `scripts/openroad-docker.sh`, as the
  2026-09-09 attempt did). The SPEF-annotated STA session of this same
  route reports, at the SDF's own corner (`tt_025C_1v80`): setup worst
  slack **+3.64187 ns** (TNS 0), hold worst slack **+0.36598 ns** (TNS 0)
  — the design closes both setup and hold at the annotated corner
  (`modexp_route_corner_tt_025C_1v80_metrics.json`, frozen in the P&R
  scratch tree this record's recipe reproduces). **The re-run does not
  reproduce `layout/modexp.gds` byte-for-byte** — the non-reproducibility
  finding of `verification/records/drc-lvs/records/20260909-230959-92e00f2.md`
  carries forward unchanged: the SDF and netlist here describe a fresh
  route of the same frozen netlist/floorplan/seed/power request, not the
  committed GDS.
- **Run configuration**:
  1. `./scripts/setup-env.sh` at this record's bumped `klt` pin
     (`dac2b5daceb6`, `docs/environment.md`'s issue-#133 rationale — live
     ancestry to the #1857/#2304 fix commits verified there). Icarus 13.0
     built from the upstream `v13_0` tag on this host (Ubuntu 24.04's apt
     ships 12.0, which `options.sdf` refuses; recipe in
     `docs/environment.md`).
  2. `klt place-and-route` on the scratch request (frozen as
     `artifacts/20260923-054800-e26f603/par-modexp-sdf.json`; response as
     `par-output.json`). `write_verilog` → `modexp_post_route_sdf.v`,
     `write_sdf` → `modexp_route.raw.sdf` (1,928 `INTERCONNECT`, 1,954
     `IOPATH`, 130 `TIMINGCHECK` entries; header carries min::max only, so
     `options.sdf.corner` is `"min"`).
  3. **SDF subset filter** (the load-bearing disclosure of this record):
     from the raw SDF, drop (a) the 4 `ANTENNA_<N>.DIODE`-destination
     `INTERCONNECT` entries — every one `(0.000:0.000:0.000)`, every one a
     physical-only cell whose no-power model is an empty module
     (`sky130_fd_sc_hd__diode_2`'s third variant), unresolvable by Icarus
     (`Could not find intermodpath!`) exactly as the 2026-09-09 attempt
     found; and (b) every `IOPATH` line, removing emptied `(DELAY
     (ABSOLUTE))` shells and content-free `CELL` blocks structurally
     (paren balance verified). Result: 1,924 `INTERCONNECT` + 130
     `TIMINGCHECK` entries, 0 `IOPATH` (frozen as `modexp_route.sdf`;
     input raw SDF frozen as `modexp_route.raw.sdf`). The filter's
     justification for (b) is the isolation table below, not convenience.
  4. Sim directory assembled per the 2026-09-09 cold-start recipe (symlinked
     unmodified `test_modexp.py`/`_dut.py`, PDK cell models via
     `klt pdk find`, `sky130_fd_sc_hd_sdf_defines.v` with `FUNCTIONAL`
     left undefined — `klt` rejects the combination otherwise); request
     frozen as `request-modexp-gate-level-sdf.json`.
- **Statistical convention**: 3 consecutive runs of the identical request
  (fixed `random_seed: 1`), byte-identical envelopes — determinism
  confirmed rather than assumed, because the full-annotation failure mode
  below was observed to be run-to-run nondeterministic mid-diagnosis
  (matching the testbench-sensitivity klayout-tools #1619 §3.7 recorded).
- **Result**: **`klt functional-verification` envelope: `status: "pass"`,
  `test_count: 2, passed_count: 2, failed_count: 0`,
  `environment.sdf.annotated: true`, `partial: true`,
  `environment.sdf.dropped.timingcheck.count: 129`** (129 vs the SDF's 130
  sections is the transcript-interleaving artifact the 2026-09-09 record
  documented — count with `grep -c '^SDF WARNING'`, which yields 130).
  Transcript: **0** `Could not find intermodpath` diagnostics. Simulated
  times are **identical to the RTL and Leg-1 zero-delay runs**
  (`test_modexp_known_vectors` 23750.0 ns, `test_modexp_random` 180480.0
  ns): every annotated net delay is sub-ns against a 10 ns clock, so no
  transition crosses a clock edge — the annotated run is cycle-for-cycle
  identical to the zero-delay run while carrying the real net delays. No
  test passes zero-delay and fails annotated (the finding class item 7
  asks to record): none exists on this netlist at this corner.

  **IOPATH exclusion — the isolation evidence** (all rows same netlist,
  same models, same 10 ns clock unless noted; diagnostic scratch runs,
  their decisive outputs frozen alongside this record):

  | SDF handed to `$sdf_annotate` | Outcome |
  |---|---|
  | none (zero-delay models) | regression advances; state machine cycles normally |
  | `INTERCONNECT` entries only (this record's SDF) | **regression passes, correct results, 3× deterministic** |
  | `IOPATH` entries only | dead — FSM never leaves `S_IDLE`, `done` never asserts |
  | `INTERCONNECT` + combinational `IOPATH` (sequential `CELL` blocks removed) | dead |
  | full `IOPATH` + `INTERCONNECT` | 4 antenna `INTERCONNECT` errors, then dead |
  | full annotation, **10× relaxed clock (100 ns)** | **still dead** (`control-100ns-clock-full-annotation-test_icarus.log.txt`) |

  A genuine timing failure cannot survive a 10× period relaxation on a
  design with +3.6 ns setup / +0.4 ns hold slack; a dead design at both
  periods with net-delay-only annotation passing is an
  annotation-mechanism kill. This is the discriminating case
  [klayout-tools#1888](https://github.com/2AMLogic/klayout-tools/pull/1888)'s
  "genuine timing outcome" attribution lacked — its repro design had
  −2.05 ns worst slack, where a real timing failure and an annotation kill
  are indistinguishable. Suspected mechanism (per #1854's original
  hypothesis, which this evidence revives): the sky130 `specify`-branch
  models wire each cell's functional UDP through `*_delayed` nets that
  Icarus replaces with "copies", and annotated module-path (`IOPATH`)
  delays on top of that fallback leave some cells settling to stale
  values. Filed generically as
  [klayout-tools#2364](https://github.com/2AMLogic/klayout-tools/issues/2364);
  the antenna residual as
  [klayout-tools#2363](https://github.com/2AMLogic/klayout-tools/issues/2363).
  The two superseded attempt envelopes (full annotation: 4-diagnostics
  error, and the dead-design pass) are frozen as
  `full-annotation-4-intermodpath-diagnostics.json` and
  `full-annotation-dead-design-envelope.json`.
- **klt provenance**: `klt` 0.6.0+gdac2b5daceb6 @
  `dac2b5daceb69a2068d9d2ee190d7afe37b29af7` for `place-and-route`
  (SPEF/SDF export) and `functional-verification` (SDF back-annotation).
  Icarus Verilog 13.0 (stable) (`v13_0` source build, per
  `docs/environment.md`'s SDF requirement note). PDK: sky130A
  (`open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b`). OpenROAD
  `26Q3-1260-g06a5a02279` via the pinned `openroad/orfs` image.
- **Links**:
  - Superseded record: `verification/records/gate-level-sim/records/20260911-071039-ce2b24c.md`
  - Claim document: `verification/gate-level/README.md` ("Leg 2" section,
    updated by this issue)
  - Fixed upstream: [klayout-tools#1619](https://github.com/2AMLogic/klayout-tools/issues/1619)
    (closed), fixed by
    [#1857](https://github.com/2AMLogic/klayout-tools/pull/1857) and
    [#2304](https://github.com/2AMLogic/klayout-tools/pull/2304) — the
    49-entry residual of the 2026-09-09 attempt
  - New findings, filed generically (no design-specific detail) per
    `CLAUDE.md`'s friction protocol:
    [klayout-tools#2363](https://github.com/2AMLogic/klayout-tools/issues/2363)
    (zero-delay antenna-diode `INTERCONNECT` unresolvable — propose
    extending #2304's counted drop) and
    [klayout-tools#2364](https://github.com/2AMLogic/klayout-tools/issues/2364)
    (IOPATH annotation kills a timing-clean design; #1888's timing
    attribution does not generalize)
  - **T1 item 7 citation**: `verification/signoff/block-manifest.json`'s
    item-7 entry cites the passing envelope
    `verification/records/gate-level-sim/artifacts/20260923-054800-e26f603/klt-functional-verification.json`
    (`options.sdf` set, `environment.sdf.annotated: true`,
    `status: "pass"`); the regenerated `verification/signoff/tier-report.json`
    (via `verification/signoff/run-signoff.sh`, `--check`-stable) grades
    **item 7 `met`** — T1 items met moves 1/11 → 2/11. Cited as a bare
    path, not a `content_hash` pin: a `functional-verification` envelope
    records no input hash by design, and a pinned hash renders
    `unverifiable_provenance` (klayout-tools #2182's rule)
  - Raw artifacts (frozen at record time): `artifacts/20260923-054800-e26f603/`
    — `par-modexp-sdf.json`, `par-output.json`, `modexp_post_route_sdf.v`,
    `modexp_route.raw.sdf` (raw `write_sdf` output), `modexp_route.sdf`
    (the annotated subset actually consumed), `request-modexp-gate-level-sdf.json`,
    `sky130_fd_sc_hd_sdf_defines.v`, `klt-functional-verification.json`
    (the passing envelope), `test_icarus.log.txt` / `results_icarus.xml.txt`
    (`.txt` suffixes to bypass `.gitignore`'s `*.log`/scratch patterns —
    committed evidence, matching the 2026-09-09 record's convention),
    `full-annotation-4-intermodpath-diagnostics.json`,
    `full-annotation-dead-design-envelope.json`,
    `control-100ns-clock-full-annotation-test_icarus.log.txt`
- **Timestamp / author**: 2026-09-23T05:48:00Z, loom-builder (issue #133)
- **Supersedes**: 20260911-071039-ce2b24c
