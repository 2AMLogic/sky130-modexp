<!-- record-meta
{
  "record_id": "20260816-175146-a084639",
  "experiment": "gate-level-sim",
  "supersedes": "20260815-124425-3a90030",
  "git_revision": "a0846397ffa81bca64f335807f835b151523bfbc",
  "provenance": {
    "klt_version": "0.2.0",
    "pdk": {
      "name": "sky130A",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "n/a",
      "content_hash": "n/a",
      "note": "no DRC/extraction deck is involved in a simulation run; the extraction deck that produced this record's input netlist is recorded in verification/records/drc-lvs/ (sha256:ec2b16dc7fc43cec24782a47531e8aa744160d487e5c0523523db5d600ad9935)",
      "cell_models": [
        {
          "path": "<sky130A>/libs.ref/sky130_fd_sc_hd/verilog/sky130_fd_sc_hd.v",
          "content_hash": "sha256:12d5d6448ccf265c544d88402d5c10ab6de9bee8b8cde48eee1f31ea8c3c3e45"
        },
        {
          "path": "<sky130A>/libs.ref/sky130_fd_sc_hd/verilog/primitives.v",
          "content_hash": "sha256:7d4b1441d06b0f2b9662befa675a0484d77149e04eb42a5f1fa2d4cfded7b2ce"
        }
      ],
      "cell_model_note": "not under version control in this repo -- pinned transitively by pdk.version (open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b); resolve with `klt pdk find --pdk sky130A`. Hashes measured on the environment this record was produced on, so staleness is detectable by hand even though check_records.py cannot recompute a path outside the working tree. Identical to the superseded record's hashes -- same pinned PDK version, unchanged content.",
      "cell_model_defines": ["FUNCTIONAL", "UNIT_DELAY=#1"],
      "cell_model_use_power_pins": false
    },
    "inputs": [
      {
        "path": "layout/modexp.gds",
        "content_hash": "sha256:229ed6a5f92938699acc969f757d84b9348731bacbe12a485473161e917c8d10"
      },
      {
        "path": "layout/lvs/modexp_layout_abstracted.spice",
        "content_hash": "sha256:04748e40b81d7e623dde2ed380be0d61cc097e57b0ff21bcd312f3f0a0691e0b"
      },
      {
        "path": "layout/lvs/modexp_layout_extract_report.json",
        "content_hash": "sha256:2f7b948d83db7ed0e506e59bc86360b8a886a73244fd5c7f2a7b1eeedd8439bd"
      },
      {
        "path": "verification/gate-level/spice_to_verilog.py",
        "content_hash": "sha256:585caadee357dadea81642eedbca7c81abd76f9f0ddbf4f5327e0d20447e3240"
      },
      {
        "path": "scripts/spice_lef_parsing.py",
        "content_hash": "sha256:96ead29c8dd50cd2a6f3ecbc565ed5d4470c56206510a82342817396500f1202"
      },
      {
        "path": "verification/gate-level/modexp_post_route.v",
        "content_hash": "sha256:2551df0bcdb55c12d2d86dfbc62d7c3f40ad87856ce8b9b08dc5c2249bf13206"
      },
      {
        "path": "verification/gate-level/sky130_fd_sc_hd_sim_defines.v",
        "content_hash": "sha256:77dea4c4aa8230209c29f1157f136e9f529ec8536bceec917e6cdc065806a9cb"
      },
      {
        "path": "verification/gate-level/request-modexp-gate-level.json",
        "content_hash": "sha256:52889553cb8a7fd452d05d65fe237c51a0380a3d92eb4d69b6bbb6f9df2b22c9"
      },
      {
        "path": "verification/gate-level/gate_level_cross_check.py",
        "content_hash": "sha256:50328ea61161cc62183c7c7cdc2fc73bee4d8b1aa5f73babda971bb5682962b6"
      },
      {
        "path": "verification/test_modexp.py",
        "content_hash": "sha256:d5993783035bad3f8299a5e378c9e8334e4e4cedd86bb86e23a8fe0bb29e4acd"
      },
      {
        "path": "verification/cross_check_tb.py",
        "content_hash": "sha256:34bb7a2b032eefd664a2e75c04ff88e457000e9391e6c6d1a55cffa5d59cea9e"
      }
    ]
  }
}
-->

# Record 20260816-175146-a084639

- **Record ID**: 20260816-175146-a084639
- **Claim**: identical to the superseded record — `spec/modexp.md`'s
  Correctness row re-established against the **routed layout**, and
  `docs/baseline.md#post-route-gate-level-simulation`. Substantiates issue
  #9's Leg 1 (zero-delay gate-level equivalence). **Does not** substantiate
  #9's Leg 2 (delay-annotated simulation at the corner set), same as the
  record this one supersedes. This record exists only to re-pin
  `verification/gate-level/spice_to_verilog.py`'s provenance hash after
  issue #60's refactor, not to re-litigate the verdict.
- **Design provenance**: identical chain and netlist to the superseded
  record (`layout/modexp.gds`'s content hash is unchanged:
  `sha256:229ed6a5...e917c8d10`), re-derived here to confirm no drift:
  `layout/modexp.gds` → (issue #8's `klt extract --abstract-cells`)
  `layout/lvs/modexp_layout_abstracted.spice` → (the *refactored*
  `verification/gate-level/spice_to_verilog.py`, now importing its
  `.SUBCKT`/`MACRO` parsing from `scripts/spice_lef_parsing.py`)
  `verification/gate-level/modexp_post_route.v`. **718 instances / 59
  distinct cell types / 68 top-level pins / 770 signal + 444 power-pin nets
  = 1214 nets**, cross-checked against
  `layout/lvs/modexp_layout_extract_report.json` before the run was allowed
  to proceed — same figures as the superseded record, byte-identical
  regenerated netlist (`modexp_post_route.v` content hash unchanged:
  `sha256:2551df0b...bf13206`).
- **Run configuration**:
  1. `./verification/gate-level/run-gate-level-sim.sh` — regenerates the
     netlist (with `--check`) through the refactored converter, links the
     resolved PDK cell models, then runs `klt functional-verification
     verification/gate-level/request-modexp-gate-level.json --format json`.
     Engine `icarus` 13.0, cocotb 2.0.1, timescale `1ns/1ps`,
     `random_seed: 1`. Testbench `test_modexp` — the committed
     `verification/test_modexp.py`, reached through a git symlink
     (`verification/gate-level/test_modexp.py`, mode `120000`), not a copy.
  2. `python3 verification/gate-level/gate_level_cross_check.py
     --compare-rtl verification/records/width-cross-check/artifacts/20260815-073109-564b598/width-16.jsonl`
     — 500 randomized cases driving `verification/cross_check_tb.py`
     unmodified through cocotb's own `Runner` (same reasoning as the
     superseded record; see "klt provenance").
  - **Cause for this record, distinct from the superseded record's own
    cause**: issue #60 deduplicated `spice_to_verilog.py`'s SPICE
    `.SUBCKT`-header parsing (`logical_lines`/`parse_spice`'s first pass)
    and LEF `MACRO`/`PIN` parsing (`parse_lef_pin_directions`) — previously
    implemented independently from `layout/lvs/build_reference_netlist.py`'s
    near-identical copies — into a new shared
    `scripts/spice_lef_parsing.py` module, imported by both scripts. That
    changed `spice_to_verilog.py`'s content — and therefore its pinned
    `provenance.inputs` hash in the superseded record — with no change to
    its output: `layout/lvs/build_reference_netlist.py <verilog>
    <layout_abstracted.spice> <lef> <out>` and `spice_to_verilog.py
    <layout_abstracted.spice> <lef> <out> --check` were both re-run against
    their existing recorded inputs and diffed byte-identical against the
    committed `layout/lvs/modexp_reference.spice` and
    `verification/gate-level/modexp_post_route.v` before this record was
    minted. This record re-runs both gate-level legs to confirm that
    directly rather than asserting it.
  - Cell models: `<sky130A>/libs.ref/sky130_fd_sc_hd/verilog/sky130_fd_sc_hd.v`
    + `primitives.v`, compiled with `` `define FUNCTIONAL `` and
    `` `define UNIT_DELAY #1 ``, **without** `USE_POWER_PINS` — unchanged
    from the superseded record (identical content hashes).
  - **Corner: none** — same reasoning as the superseded record (a zero-delay
    functional run has no corner attribute; corner dependence enters only
    through SDF, which is the blocked Leg 2).
- **Statistical convention**: two independent legs, unchanged from the
  superseded record. Leg 1a is deterministic (7 directed vectors + 40
  randomized at `random_seed: 1`). Leg 1b is N = 500 randomized cases at
  `WIDTH=16`, drawn from `random.Random(20260807016)` — the same pinned
  seed derivation `verification/cross_check.py` uses, compared here against
  the current live RTL transcript
  `verification/records/width-cross-check/records/20260815-073109-564b598.md`
  (unchanged from the superseded record — this record's cause does not
  touch the RTL side). 0-mismatch pass/fail, not a distribution claim.
- **Result**:
  - **Leg 1a — `klt functional-verification`, refactored
    `spice_to_verilog.py`: PASS.** `status: "pass"`, `test_count: 2`,
    `passed_count: 2`, `failed_count: 0`. Simulated time is **identical**
    to both the RTL run's and the superseded record's
    (`test_modexp_known_vectors` 23750.0 ns, `test_modexp_random` 180480.0
    ns) — the routed design remains cycle-for-cycle identical after the
    refactor, confirming it touched only how the parsing is implemented,
    not what it produces.
  - **Leg 1b — 500-case deep run: PASS**, 500/500 match, 0 mismatches — and
    the per-vector JSONL transcript is **byte-identical** to the current
    live RTL transcript
    `verification/records/width-cross-check/artifacts/20260815-073109-564b598/width-16.jsonl`
    (same file the superseded record compared against), and byte-identical
    to the superseded record's own gate-level transcript.
  - **Netlist provenance guards: all clean, unchanged.** 718 instances (not
    683+1), 59 cell types and 68 pins matching the extraction report
    exactly, every net present in the report's `nets[]`, zero
    multiply-driven nets, zero floating cell inputs. `modexp_post_route.v`
    regenerated byte-identical to the superseded record's copy.
  - **`spice_to_verilog.py` refactor confirmed behavior-inert**: its
    `logical_lines`/`unescape` helpers and LEF `MACRO`/`PIN` parsing now
    live in `scripts/spice_lef_parsing.py` (`parse_subckt_headers()` /
    `parse_lef_macros()`), reused as-is; `parse_spice()`'s own
    top-cell/non-top split and instance-binding logic are unchanged. This
    record's `provenance.inputs` hashes match the current working tree
    (verified by `verification/check_records.py`), and both legs reproduce
    the superseded record's results byte- and cycle-for-cycle.
  - **WIDTH set reduced to 16 only, same reasoning as the superseded
    record**: the netlist is an extraction of one fixed physical layout of
    one elaboration; `WIDTH` does not survive synthesis or P&R.
  - **Leg 2 — delay-annotated simulation at the corner set: still NOT
    ACHIEVED, blocked on upstream tooling — carried forward unchanged.**
    This record's cause for existing (a stale `provenance.inputs` hash on
    `spice_to_verilog.py`, from issue #60's parsing dedup) has no bearing on
    Leg 2's blockers, so Leg 2's evidence is not re-captured here; it is
    referenced from the earlier superseded record's own frozen artifact,
    `verification/records/gate-level-sim/artifacts/20260815-072838-564b598/leg2-blocker-evidence.txt`,
    which remains accurate: no artifact carrying post-route delays exists
    (klayout-tools#1002, open) and Icarus cannot run the annotatable models
    (klayout-tools#1004, open) — not re-verified further here, out of scope
    for this record's stale-hash fix.
  - **No mismatch or timing failure was observed anywhere in the legs that
    ran**, so there is no failing vector to report.
  - **What this run does not model** — unchanged from the superseded
    record: no parasitic extraction, no timing beyond the race-avoidance
    `UNIT_DELAY`, no corner, no power/ground network, cell-instance
    granularity (not transistor level), and not an independent check of the
    extraction itself.
- **klt provenance**: `klt` 0.2.0 at this repo's pinned revision
  `af5791b557fc7c669c3981335a294256ccf37e6f` drove Leg 1a end to end (`klt
  functional-verification`) and resolved the PDK (`klt pdk find`). PDK:
  sky130A, `open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b` — same
  pinned version and same measured cell-model content hashes as the
  superseded record. No DRC/extraction deck is involved in a simulation
  run, so `deck.content_hash` is `n/a`; the extraction deck that produced
  this record's *input* netlist is recorded in
  `verification/records/drc-lvs/`
  (`sha256:ec2b16dc7fc43cec24782a47531e8aa744160d487e5c0523523db5d600ad9935`),
  and `layout/modexp.gds`'s content hash here is identical to the one that
  record cites. Leg 1b bypasses `klt` for elaboration (informational, same
  situation as the superseded record). Simulator: Icarus Verilog 13.0,
  cocotb 2.0.1.
- **Links**:
  - Method, scope, and cell-model resolution: `verification/gate-level/README.md`
  - Converter (refactored): `verification/gate-level/spice_to_verilog.py`;
    shared parsing module: `scripts/spice_lef_parsing.py`; converter
    self-test: `verification/gate-level/test_spice_to_verilog.py` (run by
    `npm run lint`, 7/7 cases pass unmodified)
  - Runner: `verification/gate-level/run-gate-level-sim.sh`; request:
    `verification/gate-level/request-modexp-gate-level.json`
  - Deep-run driver: `verification/gate-level/gate_level_cross_check.py`
  - Refactor that caused this record: issue #60
  - Netlist under test: `verification/gate-level/modexp_post_route.v`
  - Testbenches (unchanged since the superseded record):
    `verification/test_modexp.py`, `verification/cross_check_tb.py`
  - Layout under test: `layout/modexp.gds`, `layout/lvs/README.md`
  - Upstream record this one builds on:
    `verification/records/drc-lvs/records/20260816-174540-a084639.md` (the
    extraction; also re-pinned by issue #60 for
    `build_reference_netlist.py`'s stale hash),
    `verification/records/width-cross-check/records/20260815-073109-564b598.md`
    (the current RTL transcript compared against)
  - Superseded record (frozen history, including Leg 2 blocker evidence,
    still accurate — see Result):
    `verification/records/gate-level-sim/records/20260815-124425-3a90030.md`
  - Frozen artifacts:
    `artifacts/20260816-175146-a084639/klt-functional-verification.json`,
    `artifacts/20260816-175146-a084639/run-console.txt`,
    `artifacts/20260816-175146-a084639/gate-level-width-16.jsonl`,
    `artifacts/20260816-175146-a084639/cross-check-console.txt`,
    `artifacts/20260816-175146-a084639/modexp_post_route.v`
  - Friction filed: none new (unchanged from the superseded record —
    [klayout-tools#1001](https://github.com/2AMLogic/klayout-tools/issues/1001),
    [#1002](https://github.com/2AMLogic/klayout-tools/issues/1002),
    [#1003](https://github.com/2AMLogic/klayout-tools/issues/1003),
    [#1004](https://github.com/2AMLogic/klayout-tools/issues/1004))
- **Timestamp / author**: 2026-08-16T17:51:46Z, loom-builder (issue #60)
- **Supersedes**: `20260815-124425-3a90030` — issue #60 deduplicated
  `verification/gate-level/spice_to_verilog.py`'s SPICE `.SUBCKT`-header and
  LEF `MACRO`/`PIN` parsing (previously implemented independently from
  `layout/lvs/build_reference_netlist.py`'s near-identical copies) into a
  new shared `scripts/spice_lef_parsing.py`, changing the file's content
  (and therefore its pinned `content_hash` in the record it superseded)
  with no change to test logic or simulated behavior.
  `verification/check_records.py`'s freshness check flagged the superseded
  record's `spice_to_verilog.py` hash as stale against the refactored tree.
  This record re-runs both Leg 1 legs against the refactored converter
  (behavior unchanged, confirmed byte-identical simulated times and
  transcripts) and reconfirms PASS. Leg 2's status and evidence are
  unaffected and carried forward by reference, not restated as new.
